# Hello from MDX!

This is an MDX file imported directly as a React component. 

Because we configured **Tailwind CSS**, you can use its utility classes right inside your Markdown to create rich layouts!

<div className="p-6 bg-indigo-100 dark:bg-indigo-900/30 text-indigo-900 dark:text-indigo-200 rounded-xl shadow-sm border border-indigo-200 dark:border-indigo-800 my-6">
  <h3 className="text-xl font-bold mt-0 mb-2">Tailwind in Markdown 🚀</h3>
  <p className="mb-0">This entire block is an HTML div styled with Tailwind CSS classes directly inside an `.mdx` file. It automatically responds to your dark mode setup as well!</p>
</div>

You can even import other React components here.
